export class DotstarConstants {
  static readonly url = `wss://${location.host}/api/dotstar`;
  static readonly devicePath = '/dev/null';
  static readonly fpsMax = 30;
  static readonly fpsMin = 0;
  // Broadcasts per second
  static readonly bpsMax = 30;
  static readonly bpsMin = 0;
}
